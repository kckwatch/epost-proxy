{
  "name": "standardtime-epost-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "우체국 계약고객 EMS/K-Packet OpenAPI proxy for standardtime.watch",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "test": "node --test test/epost.test.js",
    "probe:api": "node --env-file=.env scripts/probe-api.js",
    "probe:trace": "node --env-file=.env scripts/probe-trace.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "fast-xml-parser": "^4.5.1",
    "iconv-lite": "^0.6.3"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "typescript": "^5.6.3"
  }
}
